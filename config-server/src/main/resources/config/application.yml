server:
  port: 8091

spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/peroxid/aspfb.git
          repos:
            heroes:
              uri: https://github.com/peroxid/aspfb.git
              pattern: heroes-*
              searchPaths: 'config-files/{application}'