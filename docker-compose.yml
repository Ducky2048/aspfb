version: "3"

services:
  configserver:
    build:
      dockerfile: Dockerfile
      args:
        MODULE: config-server
  service-discovery:
    build:
      dockerfile: Dockerfile
      args:
        MODULE: service-discovery
  frontend:
    build:
      dockerfile: Dockerfile
      args:
        MODULE: frontend
  arena:
    build:
      dockerfile: Dockerfile
      args:
        MODULE: arena
  camp:
    build:
      dockerfile: Dockerfile
      args:
        MODULE: camp
  promoter:
    build:
      dockerfile: Dockerfile
      args:
        MODULE: promoter